#apiVersion: v1
#kind: Secret
#metadata:
#  name: teamcity-database-secret
#type: Opaque
#stringData:
#  database.properties: |
#    connectionUrl=jdbc:postgresql://{{ .Values.database.host }}:{{ .Values.database.port }}/{{ .Values.database.name }}
#    connectionProperties.user={{ .Values.database.user }}
#    connectionProperties.password={{ .Values.database.password }}
#    connectionProperties.ssl={{ .Values.database.ssl }}
